from library.print_line_generator import *

if __name__ == "__main__":
    # initialize_lineno()
    # generate_print_line("Invalid input of get_power_times_over!")
    # generate_print_line("[Solver V1] solution pointer out of legal range!")
    # generate_print_line("[Solver V1] solver failed or name not set, can't print solution!")
    # generate_print_line("[Solver V1] unknown error encountered in situation II!")
    # generate_print_line("[Solver V1] can't get disproof evidence: list is empty!")
    # generate_print_line("[Solver V1] disproof list needs more space!")
    # generate_print_line("[Solver V1] solver failed!")
    # generate_print_line("[Solver V1] solution vector format is incorrect for unknown reason!")
    
    # generate_print_line("So %s = %d (mod %d), ")
    generate_print_line("So %d ^ %s + %d = %d ^ %s is impossible.")
    
    
    
    
    
    # generate_print_line("x y z")
    
    # # print(pow(4, 68, 163))
    # # pass
    # # M = 40
    
    # # N = 10
    # # for i in range(N):
    # #     print(pow(a, i+1, M), end = " ")
    # # print("")
    # # for i in range(N):
    # #     print(pow(b, i+1, M), end = " ")
    # # print("")
    # # for i in range(N):
    # #     print(pow(c, i+1, M), end = " ")
    # # print("")
    # a = 12
    # b = 6
    # c = 18
    # A = []
    # B = []
    # C = []
    # for i in range(1, 1001):
    #     A.append(pow(a, i))
    #     B.append(pow(b, i))
    #     C.append(pow(c, i))
    # for ax in A:
    #     for by in B:
    #         cz = ax + by
    #         if cz in C:
    #             print(f"{ax} + {by} = {cz}")
                
                